const valid = require("../validator/register-validator")
const db = require("../models")

const reduceTheSpeed = async () => {
    decelerate();
    applyBreaks();
}


const decelrate = async () => {
    decelerate();
    applyBreaks();
}